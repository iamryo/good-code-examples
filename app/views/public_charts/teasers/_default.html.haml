.measure_teaser.col6.bottom_margin_base.border_grey
  .inner_padding_base
    %h4.no_margin= node_link(node)
    .top_margin_small
      = render "public_charts/teaser_charts/#{node.type}", node: node
  = link_to('View Measure', node.id, class: 'teaser_chart_link block border_top_grey inner_padding_base text_center')
